[package]
name = "spring_mass_damper"
description = "Simulates a spring damper state space."
version = "0.1.0"
edition = "2024"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1

[lib]
crate-type = ["cdylib"]

[dependencies]
nalgebra = "0.34"
fmi_rs = { path = "../../fmi_rs" }

