[package]
name = "bouncing_ball"
description = "A Rust version of the bouncing ball reference model."
version = "0.1.0"
edition = "2024"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1

[lib]
crate-type = ["cdylib"]

[dependencies]
fmi_rs = { path = "../../fmi_rs" }

