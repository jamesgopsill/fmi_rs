[package]
name = "add"
description = "Adds two numbers together using the FMI2 standard."
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
fmi_rs = { path = "../../fmi_rs" }

