[package]
name = "add_thread"
description = "Add two numbers together using threads."
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
fmi_rs = { path = "../../fmi_rs" }
